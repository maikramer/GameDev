# Text3D — Text2D + Hunyuan3D-2mini
# Instalar a partir da pasta GameDev: pip install -r config/requirements.txt
# (paths relativos: text2d em ../Text2D)

torch>=2.1.0
torchvision>=0.16.0

diffusers>=0.37.0
transformers>=4.48.0
accelerate>=0.24.0
huggingface-hub>=0.20.0

numpy>=1.24.0
Pillow>=10.0.0
trimesh>=4.0.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

click>=8.1.0
rich>=13.0.0
rich-click>=1.8.0
tqdm>=4.66.0
pydantic>=2.0.0

# Monorepo: Text2D (FLUX.2 Klein / SDNQ). Em `setup.py` vira file:// absoluto.
# CWD do pip = raiz do projecto Text3D/ — irmão Text2D em ../Text2D.
text2d @ file:../Text2D

# Hunyuan3D (hy3dgen) — licença Tencent Hunyuan Community
hy3dgen @ git+https://github.com/Tencent-Hunyuan/Hunyuan3D-2.git

# diffusers export_to_gif ainda usado em save_gif
# (Hunyuan devolve trimesh; GIF permanece utilitário opcional)

# gamedev-shared: não repetir aqui — vem como dependência do text2d (evita conflito pip file://).

# Texturização (Paint, Materialize, Upscale): pacote Paint3D separado (../Paint3D).
# Instalar: pip install -e ../Paint3D  ou  gamedev-install paint3d

# Dev dependencies movidas para pyproject.toml [project.optional-dependencies.dev]
