click>=8.0
pyyaml>=6.0
rich>=13.0
rich-click>=1.8.0

# Monorepo: biblioteca partilhada. Em setup.py vira file:// absoluto.
# Caminho relativo ao CWD do projecto (GameAssets/) — usado por pip -r com cwd=projecto.
gamedev-shared @ file:../Shared