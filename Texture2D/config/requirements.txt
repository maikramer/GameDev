# Texture2D — dependências de runtime (texturas seamless via HF Inference API)
# Uso: pip install -r config/requirements.txt
# Nota: NÃO usa PyTorch/Diffusers — geração é 100% cloud (InferenceClient).

huggingface-hub>=0.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0

click>=8.1.0
rich>=13.0.0
rich-click>=1.8.0
