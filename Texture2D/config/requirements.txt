# Texture2D — dependências de runtime (texturas seamless via FLUX.1-dev + LoRA local)
# Uso: pip install -r config/requirements.txt
# Requer PyTorch + CUDA para inferência local.

torch>=2.1.0
torchvision>=0.16.0
diffusers>=0.37.0
transformers>=4.40.0
accelerate>=0.24.0
huggingface-hub>=0.20.0
safetensors>=0.4.0
sdnq>=0.1.0
numpy>=1.24.0
Pillow>=10.0.0
python-dotenv>=1.0.0

click>=8.1.0
rich>=13.0.0
rich-click>=1.8.0

# Monorepo: biblioteca partilhada. Em setup.py vira file:// absoluto.
# Caminho relativo ao CWD do projeto (Texture2D/) — usado por scripts/setup.sh e pip -r.
gamedev-shared @ file:../Shared
