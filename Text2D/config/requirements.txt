# Text2D — dependências de runtime (text-to-2D, FLUX.2 Klein SDNQ)
# Uso: pip install -r config/requirements.txt
# Nota: torch/torchvision são instalados por scripts/setup.sh ou installer.py (CUDA/CPU).

torch>=2.1.0
torchvision>=0.16.0

# Pipeline FLUX.2 Klein (Flux2KleinPipeline); testado com 0.37.x
diffusers>=0.37.0
transformers>=4.40.0
accelerate>=0.24.0
huggingface-hub>=0.20.0
safetensors>=0.4.0

# Quantização SDNQ (registo em diffusers/transformers) — modelo Disty0
sdnq>=0.1.0

numpy>=1.24.0
Pillow>=10.0.0

click>=8.1.0
rich>=13.0.0
rich-click>=1.8.0
