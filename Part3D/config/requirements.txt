# Part3D — dependências
# gamedev-shared vem do monorepo (../Shared); instalado automaticamente.

# Core
numpy>=1.24.0
trimesh>=4.0.0
Pillow>=10.0.0
tqdm>=4.66.0

# CLI
click>=8.1.0
rich>=13.0.0
rich-click>=1.8.0

# ML/Deep Learning (torch já instalado pelo instalador)
diffusers>=0.30.0
transformers>=4.46.0
safetensors>=0.4.0
huggingface-hub>=0.20.0
optimum-quanto>=0.2.0
pytorch-lightning>=1.9.0
accelerate>=0.24.0
einops>=0.8.0
torchmetrics>=1.6.0

# Config
easydict>=1.10
omegaconf>=2.3.0
pyyaml>=6.0.0
configargparse>=1.7

# Sparse convolutions (Sonata / X-Part conditioner)
spconv-cu126

# PyG (torch-scatter, torch-cluster): não listar aqui — o instalador universal
# (gamedev-install part3d / ./install.sh part3d) instala-os após o PyTorch.

# Outros (X-Part / P3-SAM)
addict>=2.4.0
timm
numba
fpsample
scikit-learn
scikit-image
onnxruntime>=1.16.0

# 3D mesh
pymeshlab>=2023.12

# gamedev-shared (monorepo)
gamedev-shared @ file:../Shared
