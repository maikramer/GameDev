# Constraints para `gamedev-install --all` / `./install.sh --all`.
#
# Activados só quando `CLIFIED_INSTALL_ALL=1` (definido automaticamente pelo instalador).
# Objectivo: mesmo interpretador resolver `transformers` / `numpy` de forma igual em cada `.venv`,
# maximizando reutilização de rodas na cache (~/.cache/uv).
#
# Actualizar quando Rigging3D mudar `transformers` ou quando houver conflicto na resolução.

transformers==5.12.1
numpy>=2.4.5,<3
