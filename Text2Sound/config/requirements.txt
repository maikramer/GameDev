# Text2Sound — dependências de runtime (text-to-audio, Stable Audio Open 1.0)
# Uso: pip install -r config/requirements.txt
# Nota: torch/torchaudio são instalados por scripts/setup.sh ou installer.py (CUDA/CPU).

torch>=2.1.0
torchaudio>=0.16.0

# Stable Audio pipeline (modelo + inferência de difusão)
stable-audio-tools>=0.0.13

# Tensor rearrange (batch → stereo channels)
einops>=0.7.0

# Escrita de áudio portável (sem dependência de CUDA para save)
soundfile>=0.12.0

click>=8.1.0
rich>=13.0.0
rich-click>=1.8.0

# Monorepo: biblioteca partilhada. Em setup.py vira file:// absoluto.
# Caminho relativo ao CWD do projeto (Text2Sound/) — usado por pip -r com cwd=projeto.
gamedev-shared @ file:../Shared
