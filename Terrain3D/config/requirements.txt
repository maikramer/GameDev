# Terrain3D — dependências de runtime (terrain generation via terrain-diffusion)
# Uso: pip install -r config/requirements.txt
# Requer PyTorch + CUDA para inferência local.

torch>=2.4.0
torchvision>=0.19.0
diffusers>=0.30.2
transformers>=4.44.0
accelerate>=0.34.0
safetensors>=0.4.5
huggingface-hub>=0.20.0

infinite-tensor>=0.3.0
h5py>=3.12.0
scipy>=1.10.0
scikit-image>=0.24.0
pyfastnoiselite==0.0.6
rasterio>=1.4.0
numba>=0.59.0
matplotlib>=3.9.0
ema-pytorch>=0.7.0
easydict>=1.10

numpy
Pillow
tifffile
click>=8.1.0
rich>=13.0.0
rich-click>=1.8.0

# Monorepo: biblioteca partilhada. Em setup.py vira file:// absoluto.
# Caminho relativo ao CWD do projeto (Terrain3D/) — usado por scripts/setup.sh e pip -r.
gamedev-shared @ file:../Shared
