// WARNING: these are mutable shared temporaries — never store references across frames.

import * as THREE from 'three';

export const _tmpVec3 = new THREE.Vector3();
export const _tmpVec3b = new THREE.Vector3();
export const _tmpQuat = new THREE.Quaternion();
export const _tmpMat4 = new THREE.Matrix4();
